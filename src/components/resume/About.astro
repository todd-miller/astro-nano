---
import Section from "../Section.astro";

import { basics } from "@cv";
const { summary } = basics;
---

<Section title="About">
	<div>
    { 
      summary.map((item:string) => <p>{item}</p><br/>)
    }
	</div>
</Section>
